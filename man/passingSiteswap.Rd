% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/passingSiteswap.R
\name{passingSiteswap}
\alias{passingSiteswap}
\title{Passing siteswap}
\usage{
passingSiteswap(sequence = character(0))
}
\arguments{
\item{sequence}{A single character string of passing siteswap notation,
e.g. \code{"<3p 3 3 3 3 3 | 3p 3 3 3 3 3>"} or \code{"<4.5 3 3 | 3 4 3.5>"}.
Spaces within sections are ignored; \verb{<3p33|3p33>} and \verb{<3p 3 3|3p 3 3>}
are equivalent.}
}
\value{
A \code{passingSiteswap} S7 object.
}
\description{
Creates a passing siteswap object from a multi-juggler sequence in
\verb{<A|B>} notation. Each section separated by \code{|} gives one juggler's throws;
a \code{p} suffix (e.g. \code{"3p"}) marks a throw that passes to the next juggler.
Fractional notation (e.g. \code{"<4.5 3 3 | 3 4 3.5>"}) is also supported,
where \code{.5} throws are passes and the second juggler is half a beat later.
}
\details{
In p-notation, both jugglers are fully synchronised: they throw on the same
beat and alternate hands together. In fractional notation, the second juggler
is offset by half a beat in the combined timeline.
}
\examples{
passingSiteswap("<3p 3 3 3 3 3 | 3p 3 3 3 3 3>")
passingSiteswap("<4p 3 | 3 4p>")

# Compact and spaced forms are equivalent
s1 <- passingSiteswap("<3p33|3p33>")
s2 <- passingSiteswap("<3p 3 3 | 3p 3 3>")
identical(s1@throws_by_juggler, s2@throws_by_juggler)

s <- passingSiteswap("<4p 3 | 3 4p>")
s@n_props
s@valid
}
\section{Additional properties}{

\describe{
\item{\code{@type}}{Always \code{"passing"} (read-only).}

\item{\code{@is_fractional}}{\code{TRUE} for fractional notation; \code{FALSE} for p-notation.}

\item{\code{@n_jugglers}}{Number of jugglers (sections in the sequence).}

\item{\code{@sequences_by_juggler}}{List of character vectors, one per juggler, each
giving the raw tokens (e.g. \code{c("3p", "3", "3")}).}

\item{\code{@throws_by_juggler}}{List of numeric vectors, one per juggler, of throw
heights (integers for p-notation; may include \code{.5} for fractional).}

\item{\code{@is_pass_by_juggler}}{List of logical vectors, one per juggler, indicating
which throws are passes.}

\item{\code{@period}}{Number of throws per juggler per cycle.}

\item{\code{@symmetry}}{\code{"symmetrical"} when period is odd; \code{"asymmetrical"} when
period is even.}

\item{\code{@n_props}}{Total number of props (\verb{sum(all throws) / period}).}

\item{\code{@can_throw}}{\code{TRUE} if no two throws land on the same
\verb{(juggler, beat, hand)} triple (no collision).}

\item{\code{@satisfies_average_theorem}}{\code{TRUE} if \code{n_props} is a whole number.}

\item{\code{@valid}}{\code{TRUE} if both \code{can_throw} and \code{satisfies_average_theorem} are
\code{TRUE}.}
}}

