% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{ladder}
\alias{ladder}
\title{Plot a ladder diagram for a siteswap}
\usage{
ladder(
  siteswap,
  n_cycles = 3,
  direction = c("horizontal", "vertical", "h", "v"),
  title = TRUE,
  subtitle = TRUE,
  ...
)
}
\arguments{
\item{siteswap}{A \code{vanillaSiteswap}, \code{synchronousSiteswap}, or \code{multiplexSiteswap} object.}

\item{n_cycles}{Number of complete cycles to simulate (default 3).}

\item{direction}{Orientation of the diagram: \code{"horizontal"} (default, time
runs left to right) or \code{"vertical"} (time runs top to bottom). Shorthands
\code{"h"} and \code{"v"} are also accepted.}

\item{title}{Logical. If \code{TRUE} (default), adds a title showing the siteswap
sequence. Set to \code{FALSE} to suppress; override with \code{\link[ggplot2:labs]{ggplot2::labs()}} on
the returned plot.}

\item{subtitle}{Logical. If \code{TRUE} (default), adds a subtitle showing the
siteswap type and number of props. Set to \code{FALSE} to suppress; override
with \code{\link[ggplot2:labs]{ggplot2::labs()}} on the returned plot.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A ggplot2 object.
}
\description{
Produces a ladder diagram showing prop paths between hands across beats.
Straight lines indicate self-throws (same hand catches); crossing lines
indicate cross-throws (opposite hand catches).
}
\examples{
s <- vanillaSiteswap("531")
ladder(s)

}
